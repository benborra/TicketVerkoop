@model Ticket.Model.Clubs

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Clubs</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.naam)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.naam)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Stadionid)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Stadionid)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.logo)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.logo)
        </dd>

    </dl>

    @if (TempData["cantDelete"] != null)
            {
                TempData["cantDelete"] = null;
        <div class="alert alert-danger">
            <strong>Opgelet!</strong> Deze Club heeft nog wedstrijden en kan niet worden verwijdered, verwijder eerst de wedstrijden voor je de club verwijderd. <br />

        </div>
    }
    else
    {
        using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    }

</div>
